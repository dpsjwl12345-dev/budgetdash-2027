# 폰트 확대 개선본 검증 기록

- 1440×900 데스크톱 캡처에서 네이비 사이드바·헤더·요약 카드·검토 큐·예산 테이블의 구조가 정상적으로 렌더링됨.
- 기존 참조 화면보다 페이지 제목, 버튼 라벨, 요약 카드 수치, 필터 라벨, 테이블 본문과 상태 배지의 글자 크기를 키웠고, 행 높이와 카드 높이를 함께 조정함.
- 검색 입력에 `노인일자리`를 입력했을 때 10개 항목 중 1개 항목만 표시되어 검색 흐름이 정상 작동함.
- 넓은 테이블은 데스크톱 업무 화면을 전제로 가로 스크롤을 허용함. 모바일 재배치는 범위에서 제외함.
- 정원·현원 요약 버튼을 눌렀을 때 `정원·현원 편집` 모달이 열리고 14명/12명 입력값, 결원 안내, 취소·저장 버튼이 표시됨.
- 모달에서도 제목과 입력 영역의 글자가 확대되어 데스크톱에서 읽기 편한 상태임.
- 오류 필터를 선택하면 4개 오류 항목만 남고 상태선·상태 배지·검토 안내가 유지되어 검토 대상 좁히기가 정상 작동함.

## 데이터 입력 기능 검증

- 예산 행의 편집 버튼을 누르면 정책, 세부사업, 산출내역, 요구액, 전년도 금액, 시비·국비·도비·기타 재원, 상태, 검토 메모를 수정할 수 있는 모달이 열림.
- 요구액을 290,000에서 291,000으로 변경하고 저장했을 때 해당 행이 291,000으로 갱신되고 요구액 요약 및 합계가 4,952로 자동 갱신됨.
- 상단 편성 부서 입력창에 `기획예산과`를 입력했을 때 표 제목이 `2027년 편성 · 기획예산과`로 즉시 갱신됨.
- 엑셀 양식 다운로드는 XLSX 파일을 생성하며, 엑셀 업로드는 `.xlsx`, `.xls`, `.csv` 파일 입력을 받아 첫 번째 시트의 행을 예산 항목으로 변환하도록 구현됨.
